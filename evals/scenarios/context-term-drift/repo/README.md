# parcel-cli

Track every delivery from the terminal. A delivery is created, dispatched, and
finally arrives; `parcel status <id>` prints where each delivery currently is.

## Usage

    parcel create <id> <destination>
    parcel status <id>
