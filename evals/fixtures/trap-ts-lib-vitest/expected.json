{
  "resolution": "single",
  "surfaces": [
    {
      "stack": "node-backend",
      "role": "primary"
    }
  ],
  "package_json": [{"path": "package.json", "roles": ["build-tooling", "plain-node"]}],
  "forbidden_stacks": [
    "frontend-web"
  ],
  "regression_for": "design-decisions 2026-07-07: vitest dep pulled 'vite' substring match -> pure TS lib misdetected as frontend",
  "notes": "vitest/typescript add build-tooling while the package still reaches plain-node; root paths are exactly package.json."
}
