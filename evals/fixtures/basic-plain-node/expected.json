{
  "resolution": "single",
  "surfaces": [
    {
      "stack": "node-backend",
      "role": "primary"
    }
  ],
  "package_json": [{"path": "package.json", "roles": ["plain-node"]}],
  "notes": "No role signal except the plain-Node fallback; root paths are exactly package.json."
}
