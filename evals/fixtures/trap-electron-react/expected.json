{
  "resolution": "single",
  "surfaces": [
    {
      "stack": "electron",
      "role": "primary"
    }
  ],
  "package_json": [{"path": "package.json", "roles": ["ui-framework", "build-tooling", "desktop"]}],
  "forbidden_stacks": [
    "frontend-web"
  ],
  "regression_for": "table note: desktop checks MUST precede the frontend check (renderers depend on react/vue too)"
}
